#include <iostream>
#include <ctime>
#include <windows.h>
#include <conio.h>

using namespace std;

int main()
{
	srand(time(NULL));
	int shipPos[12][24] = { 0 }; //двовимірний масив для прязки координат кораблів на мапі
	int enemyShipPos[12][24] = { 0 }; //двумерный массив благодоря которому можно будет отслеживать:
}
